{% extends "partials/crm.html.twig" %}

{% block title %}{% trans %}List of categories{% endtrans %}{% endblock %}

{% block breadcrumb_parent_url %}{{ url('crm_category_list') }}{% endblock %}
{% block breadcrumb_parent_name %}{% trans %}Categories{% endtrans %}{% endblock %}
{% block breadcrumb_section_name %}{% trans %}List{% endtrans %}{% endblock %}

{% block content %}
    <div class="row mb-2">
        <div class="col-sm-4">
            <a href="{{ url('crm_category_create') }}" class="btn btn-info mb-2"><i class="fas fa-plus"></i> {% trans %}Add{% endtrans %}</a>
        </div>
    </div>
    {% for message in app.flashes('message') %}
        <div class="alert alert-success">
            {{ message }}
        </div>
    {% endfor %}
    <div class="card mb-3">
        <div class="card-header">
            <i class="fas fa-table"></i>
            {% trans %}List of categories{% endtrans %}
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-bordered" id="dataTable">
                    <thead>
                        <tr>
                            <th style="width: 80%">{% trans %}Name{% endtrans %}</th>
                            <th style="width: 20%"></th>
                        </tr>
                    </thead>
                    <tfoot>
                        <tr>
                            <th style="width: 80%">{% trans %}Name{% endtrans %}</th>
                            <th style="width: 20%"></th>
                        </tr>
                    </tfoot>
                    <tbody>
                    {% set counter = 0 %}
                    {% for category in categories %}
                        {% set counter = counter + 1 %}
                        <tr>
                            <td style="width: 80%">{{ category.name }}</td>
                            <td style="width: 20%">
                                <a href="#" data-toggle="modal" data-target="#removeElementModal-{{ counter }}" class="dropdown-item"><i class="fas fa-trash"></i> {% trans %}Remove{% endtrans %}</a>

                                <div class="modal fade" id="removeElementModal-{{ counter }}" tabindex="-1" role="dialog" aria-labelledby="removeElementModal" aria-hidden="true">
                                    <div class="modal-dialog" role="document">
                                        <div class="modal-content">
                                            <div class="modal-header">
                                                <h5 class="modal-title" id="exampleModalLabel">{% trans %}Are you sure?{% endtrans %}</h5>
                                                <button class="close" type="button" data-dismiss="modal" aria-label="Close">
                                                    <span aria-hidden="true">×</span>
                                                </button>
                                            </div>
                                            <div class="modal-body">{% trans %}Select "Remove" below if you are ready to delete the category.{% endtrans %}</div>
                                            <div class="modal-footer">
                                                <button class="btn btn-secondary" type="button" data-dismiss="modal">{% trans %}Cancel{% endtrans %}</button>
                                                <a class="btn btn-danger" href="{{ url('crm_category_delete', {'name': category.name}) }}">{% trans %}Remove{% endtrans %}</a>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="2">{% trans %}Empty list{% endtrans %}</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}