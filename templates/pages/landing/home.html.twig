{% extends "partials/landing.html.twig" %}

{% block title %}Home{% endblock %}
{% block content %}
    {# TODO: Add buttons format and a big image with a short description #}
    <div class="card card-login mx-auto mt-5">
        <div class="card-header">Home</div>
        <div class="card-body">
            <div class="text-center">
                <a class="d-block small mt-3" href="{{ path('register')}}">Register an Account</a>
                <a class="d-block small" href="{{ path('forgot-password')}}">Forgot Password ?</a>
            </div>
        </div>
    </div>
{% endblock %}